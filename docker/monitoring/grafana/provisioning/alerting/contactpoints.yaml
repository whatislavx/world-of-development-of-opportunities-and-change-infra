apiVersion: 1

contactPoints:
  - orgId: 1
    name: slack-default
    receivers:
      - uid: slack-default
        type: slack
        settings:
          url: "${SLACK_WEBHOOK_URL}"
          recipient: "#alerts"
          username: "grafana"
          icon_emoji: ":grafana:"
